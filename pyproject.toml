[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "HornCAD"
version = "0.0.0"
description = "Browser-based horn and waveguide geometry tool."
requires-python = ">=3.13,<3.14"
dependencies = [
    "trimesh>=4.0",
]
