# Agent Notes

- Before modifying files, use git to create or verify a recoverable checkpoint. Keep changes tracked so failed experiments can be reverted precisely.
- Read `docs/AGENT_HANDOFF.md` before changing geometry, widgets, STL exporters, or mesh generation.
